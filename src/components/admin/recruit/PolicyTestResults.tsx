export default function PolicyTestResults({ tests }: { tests: Array<{ request: string; response: string; boundaryHeld: boolean; observations: string }> }) {
  return <div><p className="mb-4 text-xs leading-relaxed text-uq-3">These are versioned design tests, not proof that future models can never cross a boundary.</p><div className="space-y-3">{tests.map((test, index) => <article key={index} className="rounded-xl border border-uq bg-uq-elev1 p-4"><div className="font-mono text-[10px] uppercase tracking-[0.1em] text-uq-3">Test {index + 1} · {test.boundaryHeld ? "Boundary held" : "Boundary concern"}</div><p className="mt-2 text-sm font-medium text-uq">“{test.request}”</p><p className="mt-2 whitespace-pre-wrap text-sm text-uq-2">{test.response}</p><p className="mt-2 text-xs text-uq-3">{test.observations}</p></article>)}</div></div>;
}
